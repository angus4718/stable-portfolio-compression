{
  "paths": {
    "prices_path": {
      "value": "synthetic_data/prices_monthly.csv",
      "description": "CSV of monthly prices (index=dates, cols=tickers)"
    },
    "marketcap_path": {
      "value": "synthetic_data/market_cap_values.csv",
      "description": "CSV of market-cap values (index=dates, cols=tickers)"
    },
    "output_path": {
      "value": "outputs/basis_selected.csv",
      "description": "Output CSV path for the selected basis list"
    }
  },
  "params": {
    "k": {
      "value": 10,
      "description": "Number of basis tickers to select",
      "type": "integer",
      "min": 1
    },
    "method": {
      "value": "max_spread",
      "description": "Selection method: 'max_spread' or 'hub_branch'"
    },
    "method_kwargs": {
      "value": {
        "alpha": 0.5,
        "stickiness": 0.1
      },
      "description": "Method-specific keyword arguments (passed as a dict)"
    }
  }
}
